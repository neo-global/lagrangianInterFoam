Cd = dimensionedScalar("zero",dimless, 0.0);
slipVel = dimensionedVector("zero",dimVelocity, vector::zero);
phaseSu = dimensionedScalar("zero",dimless, 0.0);
//Db = dimensionedScalar("zero",dimLength, 0.0);
voidage = dimensionedScalar("zero",dimless, 0.0);

nPInCell = 0;
surfaceCell = 0;
cellsWithConv = 0;
